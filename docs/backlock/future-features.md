# Legacy Parking Lot Copy

This directory exists only because the repo already had a typo-folder copy under `docs/backlock/`.

The authoritative parking-lot document is:

- `docs/backlog/future-features.md`

Do not update this file unless the repo intentionally chooses to maintain two parallel copies again.
